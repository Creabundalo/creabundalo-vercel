# Server-only. Never commit real values.
SCW_ACCESS_KEY=
SCW_SECRET_KEY=
SCW_OBJECT_BUCKET=
SCW_OBJECT_REGION=nl-ams

# JSON map: SHA-256(sync-token) -> pseudonymous owner id
CREA_VAULT_SYNC_USERS={}

# Optional comma-separated origins for future cross-origin clients.
CREA_ALLOWED_ORIGINS=
